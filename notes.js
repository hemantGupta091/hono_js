//console.log('app notes start here!!');

//module.exports.age = 25;
/*module.exports.addNote = () => {
	console.log('My arrow function');
	return 'new note';
};

module.exports.add = (a,b) => {
	return a + b;
};*/

/*var addNote = (title, body) => {
	console.log('My title '+ title +'and ' + body);
};

var getAll = () => {
	console.log('Get all notes');
};

var getNote = (title) => {
	console.log('read notes');
};

var removeNote = (title) => {
	console.log('remove notes');
};

module.exports = {
	addNote,
	getAll,
	getNote,
	removeNote
	
	
};*/

const fs = require('fs');
var addNote = () =>{
 	var notes = [];

 	note = {
 		title,
 		body
 	};

}


